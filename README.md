# Training Portal Prototype

Minimal Vite React scaffold to run the provided `training_portal_prototype.jsx` UI.

Quick start:

1. Install dependencies:

```bash
cd TrainingPortalPrototype
npm install
```

2. Run dev server:

```bash
npm run dev
```

Open http://localhost:5173
